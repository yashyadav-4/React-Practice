import InputBox from "./InputBox";

export {InputBox};

// this file is created bcz it makes easier for us to import all components together from one file to app.jsx (in this file we import all components we need than export them together ) makes app.jsx clean for bigger projects
